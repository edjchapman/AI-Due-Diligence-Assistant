node_modules/
dist/
.env
*.log
coverage/

# Stray local SQLite files. This project's datastore is Postgres/pgvector, so a
# *.db file is never a real artefact — just a scratch DB from an ad-hoc tool.
*.db

# Claude Code — ignore only the personal/machine-specific bits (symlinks + local
# settings). Deliberately NOT a blanket .claude/ ignore, so a committed
# .claude/settings.json + .claude/hooks/ bootstrap can still be tracked.
.claude/agents
.claude/commands
.claude/skills
.claude/rules
.claude/settings.local.json
.mcp.json

# JetBrains IDE
.idea/

# Personal work-logs (from the /standup skill) — kept local, off the public repo
standups/

# Added by ggshield
.cache_ggshield
